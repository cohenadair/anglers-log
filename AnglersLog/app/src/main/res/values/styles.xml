<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="android:logo">@android:color/transparent</item>

        <item name="android:actionBarStyle">@style/ActionBar</item>
        <item name="actionBarStyle">@style/ActionBar</item>

        <!-- For activated list items -->
        <item name="android:activatedBackgroundIndicator">@drawable/list_item_selected</item>
    </style>

    <style name="ActionBar" parent="@style/Widget.AppCompat.Light.ActionBar">
        <item name="android:background">@color/anglers_log_theme</item>
        <item name="background">@color/anglers_log_theme</item>
    </style>

</resources>
