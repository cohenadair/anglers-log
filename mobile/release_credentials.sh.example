#!/usr/bin/env bash
# Template for release_credentials.sh — copy this file to
# release_credentials.sh (gitignored) and fill in real values before running
# build_and_upload_release.sh.
#
#   cp release_credentials.sh.example release_credentials.sh

export APPLE_ID="TODO"
export APPLE_APP_SPECIFIC_PASSWORD="TODO"
export APPLE_TEAM_ID="TODO"
export GOOGLE_PLAY_JSON_KEY="../../google-play-release-pipeline-key.json"
